version: 0.0
os: linux

files:
  - source: /
    # ec2 목적지
    destination: /home/ec2-user/myapp/lifepost/service
    orverwirte: yes

permissions:
  - object: /
    parttern: "**"
    owner: ec2-user
    group: ec2-user

hook:
  ApplicationStart:
    - location: ./deploy.sh
      timeout: 30
      runas: root